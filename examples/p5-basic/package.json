{
	"name": "@example/p5-basic",
	"version": "0.0.1",
	"description": "Basic p5.js & GenArtAPI example",
	"repository": "https://github.com/thi-ng/genart-api",
	"author": "Karsten Schmidt (https://thi.ng)",
	"license": "MIT",
	"type": "module",
	"scripts": {
		"build:prepare": "node ../../tools/copy-lib.js",
		"start": "yarn build:prepare && vite --host --open",
		"build": "yarn build:prepare && vite build --base='./'",
		"preview": "vite preview --host --open"
	},
	"dependencies": {
		"@genart-api/core": "workspace:^"
	},
	"devDependencies": {
		"vite": "^6.3.5",
		"vite-plugin-html": "^3.2.2"
	}
}
