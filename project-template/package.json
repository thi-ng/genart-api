{
	"name": "TODO",
	"version": "0.0.1",
	"description": "Basic GenArtAPI project template",
	"repository": "https://github.com/thi-ng/genart-api",
	"author": "TODO",
	"license": "MIT",
	"type": "module",
	"scripts": {
		"build:prepare": "cp node_modules/@thi.ng/genart-api/*.js lib",
		"start": "yarn build:prepare && vite --host --open",
		"build": "yarn build:prepare && tsc && vite build --base='./' && cp -R lib dist",
		"preview": "vite preview --host --open"
	},
	"devDependencies": {
		"typescript": "^5.6.3",
		"vite": "^5.4.8",
		"vite-plugin-html": "^3.2.2"
	},
	"dependencies": {
		"@thi.ng/genart-api": "^0.7.1"
	}
}
